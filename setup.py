from setuptools import setup

setup(name='Document Search',
      version='0.1',
      description='Program that search for term or phrase (single token), and return results in order of relevance',
      url='http://www.not-a-valid-domaine.com/',
      author='Kalenda Kabukala',
      author_email='kalendamoise@gmail.com',
      license='MIT',
      packages=['Document-Search'],
      zip_safe=False)
